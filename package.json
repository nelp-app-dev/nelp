{
  "name": "nelp",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:nelp-app-dev/nelp.git",
  "author": "nelp <nelp.app.dev@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start-server:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build --force-recreate api catalog cart redis postgres nginx pgadmin",
    "start-clients:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build --force-recreate admin web-sales",
    "start:dev": "yarn start-server:dev & yarn start-clients:dev"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/server",
    "packages/common",
    "packages/clients/*"
  ]
}